# API 基础 URL (使用相对路径，与后端同源)
VITE_API_BASE_URL=/api

# 前端独立演示模式：登录不依赖后端鉴权（完全后端模式关闭）
VITE_FRONTEND_ONLY=false

# 应用标题
VITE_APP_TITLE=XY Gateway (Dev)

# 安全登录入口是 Node/Vite 开发服务器的运行时配置，不要写成 VITE_* 构建变量。
# 请在项目根目录 .dev.vars 中配置 SECURE_LOGIN_ENTRY。
